/**
 * Entry point in application, that calls method to check input data
 */
public class Main {
    public static void main(String[] args) {
        Checker checker = new Checker();
        checker.chexckInput(args);
    }
}
